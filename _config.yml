port: 4000
lsi: false
permalink: /:title
markdown: kramdown
include:
gems:

# Since GH pages override this to "true", we test this value to see if we are running locally
safe: false

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"